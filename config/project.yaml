stack: pnpm
test: pnpm install && pnpm -r build && pnpm typecheck && pnpm exec vitest run
typecheck: pnpm typecheck
source:
  - src/**
  - packages/*/src/**
tests:
  - test/**
  - tests/**
  - packages/*/test/**
