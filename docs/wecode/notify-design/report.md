# notify-design → master

Written by wecode when the merge landed, from git and its own record of the
run. Generated, never authored: an agent's account of its own work is
inadmissible, and a file it could have written would be too.

```text
MERGED  notify-design → master

summary
  3 files, +250 −14
  how        signed off
  unblocks   budget-live-count
  undo       wecode rollback notify-design   (was 7bb6536de)

what changed
  crates/wecode-cli/src/notify.rs                      +200   −9
  crates/wecode-cli/tests/cli.rs                       +19    −0
  docs/reference/config.md                             +31    −5

acceptance
  ✓ `cargo test --workspace` exits 0

provenance
  branch     wecode/notify-design
  merge      9e9cc1262
  target was 7bb6536de
```
