# tui-input-lag → master

Written by wecode when the merge landed, from git and its own record of the
run. Generated, never authored: an agent's account of its own work is
inadmissible, and a file it could have written would be too.

```text
MERGED  tui-input-lag → master

summary
  1 file, +58 −5
  how        signed off
  worktree   removed /home/cws/.wecode/run/cws/tui-input-lag
  undo       wecode rollback tui-input-lag   (was 5db4f85d3)

what changed
  crates/wecode-cli/src/tui.rs                         +58    −5

acceptance
  ✓ `cargo test --workspace` exits 0
  ✓ `bash scripts/max-lines.sh` exits 0

provenance
  branch     wecode/tui-input-lag
  merge      19ba0d89a
  target was 5db4f85d3
```
