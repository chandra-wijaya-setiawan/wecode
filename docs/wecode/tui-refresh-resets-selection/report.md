# tui-refresh-resets-selection → master

Written by wecode when the merge landed, from git and its own record of the
run. Generated, never authored: an agent's account of its own work is
inadmissible, and a file it could have written would be too.

```text
MERGED  tui-refresh-resets-selection → master

summary
  1 file, +44 −2
  how        signed off
  unblocks   active-agent-shows-launcher
  worktree   removed /home/cws/.wecode/run/cws/tui-refresh-resets-selection
  undo       wecode rollback tui-refresh-resets-selection   (was d8210ed91)

what changed
  crates/wecode-cli/src/tui.rs                         +44    −2

acceptance
  ✓ `cargo test --workspace` exits 0
  ✓ `bash scripts/max-lines.sh` exits 0

provenance
  branch     wecode/tui-refresh-resets-selection
  merge      fb8feef05
  target was d8210ed91
```
