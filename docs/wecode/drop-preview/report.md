# drop-preview → master

Written by wecode when the merge landed, from git and its own record of the
run. Generated, never authored: an agent's account of its own work is
inadmissible, and a file it could have written would be too.

```text
MERGED  drop-preview → master

summary
  2 files, +87 −115
  how        signed off
  worktree   removed /home/cws/.wecode/run/cws/drop-preview
  undo       wecode rollback drop-preview   (was 1e5ad7a41)

what changed
  crates/wecode-cli/src/tui.rs                         +77    −105
  docs/reference/commands.md                           +10    −10

acceptance
  ✓ `cargo test --workspace` exits 0
  ✓ `bash scripts/max-lines.sh` exits 0

provenance
  branch     wecode/drop-preview
  merge      bf3142059
  target was 1e5ad7a41
```
