# active-agent-shows-launcher → master

Written by wecode when the merge landed, from git and its own record of the
run. Generated, never authored: an agent's account of its own work is
inadmissible, and a file it could have written would be too.

```text
MERGED  active-agent-shows-launcher → master

summary
  1 file, +44 −10
  how        signed off
  worktree   removed /home/cws/.wecode/run/cws/active-agent-shows-launcher
  undo       wecode rollback active-agent-shows-launcher   (was a3ff2bd6f)

what changed
  crates/wecode-cli/src/tui.rs                         +44    −10

acceptance
  ✓ `cargo test --workspace` exits 0
  ✓ `bash scripts/max-lines.sh` exits 0

provenance
  branch     wecode/active-agent-shows-launcher
  merge      43562e209
  target was a3ff2bd6f
```
