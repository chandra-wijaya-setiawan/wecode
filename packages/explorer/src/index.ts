/** wecode:repo-explorer — the questions wecode asks about a repository, and nothing else.
 *
 *  Scaffolding only: the port and its first adapter are story work. This file exists so the
 *  package builds before that story lands. */
export {};
