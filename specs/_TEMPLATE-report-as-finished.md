# <issue> — Report as finished

**Task:** #<number> `<task>` · **Branch:** `wecode/<task>` · **Base:** `main`

> <small>*Copy this file to `specs/<issue>-<slug>/report_as_finished.md`. The specification
> states the contract; this file tracks execution, so it changes and the specification does
> not. Generate the numbers rather than typing them — hand-counting is what produces wrong
> figures.*</small>

## 1. Summary

> <small>*The bottom line in one sentence, then the numbers behind it. State plainly what has **not** been done. End by pointing at the sections a reviewer should read first.*</small>

<!-- … -->

## 2. Requirement status — detail

> <small>*Every requirement from the specification, what satisfies it, and — the column that matters — what is and is not proven. Distinguish *met in code* from *executed*.*</small>

<!-- … -->

## 3. Repository diff

> <small>*Every file added or changed: change type, lines, the requirement it serves, status and a note. **Generate from `git diff --numstat` and `--name-status`** rather than typing counts, and audit that the row count matches the diff.*</small>

| Path | Change | Lines | FR / NFR / AC | Status | Notes |
|---|:-:|--:|---|---|---|
| | | | | | |

## 4. Blockers

> <small>*What stops progress, who owns it, and its state. Include cleared blockers so the list reads as current rather than aspirational.*</small>

| Blocker | Owner | State |
|---|---|---|
| | | |

## 5. Outstanding

> <small>*What is not done, undisguised. If something is unproven rather than unbuilt, say which.*</small>

<!-- … -->

## 6. Key decisions and justification

> <small>*Decisions taken while executing, and any awaiting ratification. Point at the specification rather than restating them.*</small>

<!-- … -->

## 7. Lessons learnt

> <small>*What the next slice should do differently, with evidence from this one — commits, measurements — rather than opinion.*</small>

<!-- … -->
