[package]
name = "wecode-core"
description = "Intent ontology, grammar and admission checks for wecode."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true

# Deliberately dependency-free. This crate is pure domain logic, which is what
# lets the admission gate be deterministic and lets the whole test suite run on
# any target. Serialization belongs to the store crate, not here.
[dependencies]

[lints]
workspace = true
